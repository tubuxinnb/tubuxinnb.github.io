services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:4000:4000"
      - "127.0.0.1:35729:35729"
    volumes:
      - .:/site
    environment:
      JEKYLL_ENV: development
    command: serve-jekyll
